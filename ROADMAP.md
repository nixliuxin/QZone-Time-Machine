# QZone-Time-Machine Roadmap

## Viewer 功能增强

### Lightbox 重新设计
**背景与性能**
- [ ] 去掉半透明 + `backdrop-blur`（卡顿且观感差），改回最初的纯灰色不透明背景（`bg-[hsl(var(--muted))]` / 纯色）
- [ ] 图片加载失败占位（`onerror` fallback，避免空白）

**信息栏（恢复完整信息，对齐原 `PhotoDetail` 面板）**
- [ ] 补回丢失字段：`desc` 描述、文件大小（`photocubage` → KB/MB）、EXIF 全部条目、评论列表
- [ ] 扩展 `LightboxMeta` 接口：增加 `desc` / `filesize` / `exif` / `comments` / `albumName` 字段
- [ ] 调用方（Photos / Blogs / Messages / Diaries）传入完整 meta，而非当前的精简子集
- [ ] 信息栏可折叠/切换（`i` 键 toggle；窄屏自动隐藏）

**导航与层级**
- [ ] 顶部面包屑/层级显示（相册名 → 当前图，可点返回上一层相册页）
- [ ] "返回上一层页面"按钮（回到相册/说说/日志列表）
- [ ] 当前位置指示（`N / 总数`）+ 首尾跳转（Home/End）
- [ ] 可选循环导航（最后一张 → 第一张）

**底部缩略图条（gallery strip）**
- [ ] 底部一排相册缩略图，左右浏览时可预知前后图
- [ ] 点击缩略图直接跳转到指定图
- [ ] 当前项高亮 + 自动滚动到可视区域

**建议补充的优化（待确认）**
- [ ] 缩放/平移：滚轮缩放、拖拽平移、双击放大/还原
- [ ] 触摸手势：移动端 swipe 左右切换、pinch 缩放
- [ ] "在新标签打开原图 / 下载原图"按钮（离线场景）
- [ ] 幻灯片自动播放（空格暂停/播放）
- [ ] 快捷键提示浮层（← → 切换，i 信息，+/- 缩放，Esc 关闭）

### 内容区分与筛选
- [ ] 说说区分原创/转发/其他类型
- [ ] 说说图片显示完整信息面板（同相册图右侧面板）
- [ ] 相册描述显示
- [ ] 照片描述/评论/POI 地点显示
- [ ] 说说地理位置、来源设备、转发数显示

### 交互与链接
- [ ] 好友分组遵循原始分组顺序

### 访客与统计
- [ ] 访客显示总访问量（官方数字）

### 搜索与发现
- [ ] 全局搜索（跨模块关键词搜索）
- [ ] 统计面板（发布频率、活跃时段、好友互动排名）
- [ ] 统一时间线（所有模块按时间混排）

### 媒体与地理
- [ ] 地图视图（有地理位置的照片聚合显示）

### 响应式与适配
- [ ] 移动端/平板响应式布局

---

## 备份引擎增强

### 增量同步
- [x] 基于唯一 ID（`tid` / `blogId` / `id`）去重 merge，而非分页偏移
- [x] 从 page 0 拉取直到遇到已有数据自动停止
- [x] 支持定期增量备份（`--incremental` 只拉取新增内容）
- [x] `mergeByIds` 通用合并工具（用于 convert 旧数据迁移 + 增量备份）
- [x] `generate-progress` 命令：为已转换数据生成合成进度文件
- [x] Enricher 幂等性：已有 comments/likes 的条目自动跳过

### 批量备份优化
- [x] `check-access` 命令：探测每位好友空间状态（accessible / no_permission / not_activated），写入状态 JSON
- [x] `backup-all --access-file` 可选项：按状态文件跳过不可访问的目标（省时、降低封号风险）
- [x] `backup-all` 自动将本人（owner）置顶加入备份列表，不遗漏自己
- [x] 备份时记录 owner 身份（`meta.json` + `user.json` 的 `owner_uin`/`is_owner`），供 viewer 精确判断 owner-only 模块
- [ ] `backup-all` 支持 `--resume-from <uin>` 从指定用户继续
- [ ] 限流自适应退避（动态调整请求间隔）
- [ ] 加密相册检测与标记（避免下载占位图）

---

## 独立桌面应用

- [ ] 打包为独立桌面 App（Electron / Tauri），用户无需安装 Node.js 或了解命令行
- [ ] 图形化登录界面（内嵌 QR 扫码窗口）
- [ ] 备份进度可视化（实时进度条 + 模块状态）
- [ ] 一键导出（选择目标文件夹即可启动完整备份）
- [ ] 自动更新机制

---

## 已完成

- [x] 键盘快捷键（← → 翻页，J/K 滚动，Esc 返回）
- [x] 数据完整性报告面板
- [x] 导航栏显示模块总量数字（官方数字）
- [x] 无数据模块自动隐藏导航项
- [x] QQ 表情本地化下载（完全离线可用）
- [x] 传统分页（URL 状态）
- [x] Light/Dark 模式切换
- [x] Lightbox 图片浏览（键盘导航）
- [x] 滚动位置保持
- [x] 好友头像显示（留言板等）
- [x] 日志/日记详情页
- [x] 相册照片右侧信息面板（EXIF）
- [x] 日志区分原创/转载标签
- [x] 日志分类标签筛选
- [x] 头像/昵称可点击跳转 QQ 空间链接
- [x] 访客显示头像
- [x] 评论区显示头像和链接
- [x] 日志完整内容获取（修复 parseBlogDetail 嵌套 div 解析）
